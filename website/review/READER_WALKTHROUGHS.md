# Reader walkthroughs for the Preskill bridge

Date: 12 September 2026. These are the assistant reviewer's own source-and-route walkthroughs, not external user testing, a new scientific audit, a browser review or an inspection of GitHub's authenticated renderer. The review read the current editorial pages, generated GitHub pages, route and learning metadata, root README, canonical model/proof, figure captions/specifications and source ledger. No project file was edited in this review.

## 1. A reader coming from the selected tutorial

Route followed: root `README.md` → `reader/README.md` → `reader/background.md#reading-map` and `#notation` → `reader/channel.md#channel-operation` → `#reference-state` → `#flagged-information` → `#coherent-repetition` → `#eight-use-witness` → `reader/proof-guide.md#two-costs`, `#finite-before-asymptotic`, `#geometric-guarantee` and `#scalar-proof` → exact M04/P01–P10 → `reader/limits.md` → `reader/verification.md`.

The root and reader overviews give the result, physical meaning and expert bypass before assigning background. The background page identifies one pinned tutorial, separates core passages from optional same-source refreshers, and gives a return destination for every passage. No additional educational source or full-chapter prerequisite is assigned.

The channel explanation supplies the missing physical bridge. It distinguishes measurement probability from conditional reporting error, lists the receiver's records and inaccessible systems together, and fixes the encoder before future branch choices. It explains the reference purification, the condition under which receiver/reference entropy equals environment entropy, and why classical-record entropy cancels in the flagged coherent-information identity. M01–M03 and P01 support those explanations.

The repetition section distinguishes a coherent encoder from copying an unknown state and distinguishes that encoder from its balanced test marginal. The eight-use section states block and per-use units, retains all nine measured-count branches including the negative all-measured contribution, and invokes the global one-use proof at the same parameters. P09 and P13 support those explanations. The proof guide then selects a finite positive inner block before outer coding, as in P10. It introduces the operational roles of Gamma and L before their geometric comparison and explicitly identifies the scalar inequality as project mathematics. The reader can explain the result without interpreting a trial input as a global optimum, a finite block as near-perfect recovery, or repetition as cloning.

## 2. An expert bypassing background

Route followed: root `README.md` expert bypass → `docs/MODEL_AND_CLAIMS.md#m04` → `docs/COMPLETE_PROOF.md` → P07/P14 and `reader/verification.md` → `reader/materials.md` → canonical certificate, three separate implementations and retained evidence. The same direct model, complete-proof, figures and materials destinations are present in the generated reader navigation; the HTML route configuration points canonical model/proof pages to those source documents.

The exact theorem and quantifier order are reachable without visiting the background page. The complete proof retains explicit P01–P15 anchors and the canonical dependency/source links. The verification guide distinguishes integrity, method tests, complete certificate verification, witness evaluation and graphical reproduction. Reading the mathematical account does not require running software. The new presentation does not turn an earlier stored result into a newly executed proof check.

## 3. Tracing displayed results and their limits

Route followed: the relevant channel/proof/limits explanation → the corresponding `reader/figures.md#figure-1`, `#figure-2` or `#figure-3` atlas section → canonical caption, named P-sections, plotting inputs and protected original downloads. The accepted-palette SVG and original PDF/SVG/PNG roles are explicit in the generated GitHub atlas. Native pages direct readers to original downloads to compare colors; interactive switching is described as an HTML-site feature.

| Display | Source trace and interpretation checked |
|---|---|
| Figure 1 and the eight-use number | P01/P13; `figure1_comparison.csv`, `figure1_all_masks_not_for_display.csv`, `figure1_original_witness140.json`. The stated rate is per physical use; all-measured loss is retained; the one-use zero uses the sharper exact threshold. No optimal-block or isolated-block-fidelity claim appears. |
| Figure 2 and the sufficient strip | P08/P10; `figure2_pauli_guaranteed_region.csv` and `cross_figure_witness_bound_check.json`. The plotted equal-Pauli slice, lower included endpoint, upper excluded endpoint and unclassified exterior remain explicit. The Figure 1 witness is correctly described as below this conservative strip. |
| Figure 3 and the small-geometry slope | P11/P12; `figure3_cone_geometry.csv` and `figure3_cone_gap.csv`. The plotted cone domain is bounded by one quarter, with the larger parameter-dependent domain separately stated. The dashed line is an analytical asymptote, not a fit. The coplanar equality concerns the construction frontiers, not an all-code converse. |

The background and contribution sections distinguish established channel/coding concepts from the project's sufficient geometric theorem and computer-assisted inequality. The current status page explains the separate unmerged correction record instead of silently restoring its historical rate/threshold assertion as current authority. Primary references remain attribution, not a second tutorial.

## Focused preservation comparison

`git diff --name-only f0015c56a19fd953c6797b2c64d9b507105234e3 -- docs certificates verification data evidence figures provenance reader/assets verify.py reproduce.py requirements.txt tests` returned no changed tracked files. The corresponding baseline tree contains 135 tracked paths. This is a file-preservation comparison, not a fresh execution of the scientific checkers. All three canonical caption bodies also occurred verbatim in the generated `reader/figures.md` during this review.

## Review outcome

No concrete source-to-reader semantic defect was found in these walkthroughs. The three requested tasks are supported by the current explanations and direct source links. The report placeholder and generated files were still being finalized by the implementation agent during this review; final generation consistency, browser behavior, CI and current remote state must be reported from their own actual checks. No external participant was tested, and this review makes no claim about learning outcomes measured in users.
