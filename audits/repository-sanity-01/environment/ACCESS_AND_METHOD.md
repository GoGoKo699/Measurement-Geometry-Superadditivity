# Access and preservation record

The target was resolved directly through the authenticated GitHub connection as private repository `GoGoKo699/Measurement-Geometry-Superadditivity`, repository ID 1361054594. Main and the pinned baseline both resolved to f0015c56a19fd953c6797b2c64d9b507105234e3. Branch search returned no `audit/repository-sanity-01`. The complete recursive tree was not truncated and contained 208 blobs; it contained no AGENTS.md, CONTRIBUTING, or separate instruction file. README and repository workflow instructions were read before scientific execution. Previous chats and external project files supplied no scientific evidence.

Direct `git ls-remote https://github.com/GoGoKo699/Measurement-Geometry-Superadditivity.git HEAD refs/heads/main refs/heads/audit/repository-sanity-01` failed with return code 128 because direct Git credentials were unavailable. No credentials were inspected, requested, or placed in commands. The existing GitHub connection successfully retrieved metadata and bytes. UTF-8 blob retrieval did not support PDF bytes; the supported file retrieval with base64 encoding did. The >1 MB certificate was retrieved through the text-blob route. One locally incomplete SVG transfer was detected by its Git hash, refetched, and verified before any source audit command. These were access/transfer issues, not repository defects.

Every file was checked against its remote Git blob SHA-1 and size. `git write-tree` reproduced 72e93a89a691ec4d2ea799bb3b1008bd6c0dee20. The GitHub signed commit was reconstructed from its returned payload and signature and verified byte-exact against the pinned SHA; an initial serialization lacking the terminal signature continuation newline failed this SHA check and was rejected. The final local commit is the actual pinned object, not a substitute snapshot commit. The local repository is shallow at that commit; unavailable older history is unnecessary to this audit. A detached baseline and separate `audit/repository-sanity-01` worktree were created. Baseline status was clean before execution.

All numerical execution used an isolated venv, pinned requirements, assertions enabled, and fresh output directories. The baseline's `build/` is ignored output space; tracked source and reference artifacts remained unchanged. Audit-only reports and evidence are added on the separate audit branch. No main update, merge, history rewrite, source fix, approval-hash replacement, visibility/license/hosting change, publication, or manuscript operation was performed. The separately prohibited project was neither accessed nor used as a dependency.

Public literature lookups used published identifiers, author names, and general topics. Private source text and numerical material were not submitted to third-party services. Website material was served only on loopback for attempted local checks. The report distinguishes source inspection, independent derivation, rigorous enclosure execution, nearest-arithmetic diagnostic calculations, historical records, and browser limitations.

The artifact belongs to the audited Git repository. Audit additions are persisted on its authorized audit branch; they are not duplicate repository copies in a separate artifact store.
