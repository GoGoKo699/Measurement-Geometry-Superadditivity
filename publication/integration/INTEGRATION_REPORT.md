# Reader, corrections and license integration

The owner authorized integration and merging after reviewing the complete candidate. The repository remains private; permission to merge does not change its visibility or approve disclosure of historical contact/project metadata. [PR #5](https://github.com/GoGoKo699/Measurement-Geometry-Superadditivity/pull/5) records the exact checked head, actual CI outcomes and observed merge result. [PUBLICATION.md](../../PUBLICATION.md) tracks the remaining publication decisions.

## Exact composition

Main started at `f0015c56a19fd953c6797b2c64d9b507105234e3`. The preparation branch started this integration at `fa65dca608e92f6816522d4ae119c2229588555e`, including reader commit `1107ac899d2e267c6cf03509168623c13db4038c` and the confirmed license adoption. The previously reviewed complete patch was applied without alteration in checkpoint `deba96a1a2fb3781a357a5d4d2660e6edda638da`, tree `1bbe9651bb1b524c2083df66374bc6a2464e6971`.

That checkpoint contains the exact nine documentary and dependent identity corrections recorded at `9ccecbf4cd3d975de2c498ba4fe2a9862f90778b`, along with the compatible reader/build adaptations. It qualifies the S6 rate/threshold comparison, corrects eight figure-input paths and removes the obsolete no-repository clause. It does not modify the mathematical argument or numerical results. The retained complete patch has SHA-256 `d752d46e89a53abdb683cd597765773ef6bc94f6ec5a16748a2977707e1ec176`.

PR #5 is retargeted directly to main, so the corrected reader and adopted licenses arrive together. The reader commit remains an ancestor; no history is rewritten. The audit branch and its reports remain available through their pinned links, without copying its historical evidence tree into the reader.

## Current maintenance and preservation

Reader prose still has one editable source in `website/pages/`, with the selected background map in `website/learning_bridge.json`. Native `reader/` files are generated. Canonical proof, captions and technical documents remain at their existing locations.

Run `python publication/integration/check_integrated.py` for the current composition. [INTEGRATED_STARTING_FILES.json](INTEGRATED_STARTING_FILES.json) pins every file at the applied-candidate checkpoint; [INTEGRATED_EDITS.json](INTEGRATED_EDITS.json) identifies the exact stage-dependent editorial and workflow changes. The website builder separately reconstructs the frozen source through the nine documented inverse substitutions and checks mathematical-source preservation and all protected graphics. The current manifest changes only the README and STATUS identities relative to the applied-candidate checkpoint.

Earlier audit, reader, adoption, binary-closure and candidate checkers and their expected hashes remain unchanged. They describe earlier commits and must not be run as if they certified a later state. The old candidate patch is preserved as evidence and is not applied again by current CI.

The lightweight integration workflow runs the standard-library current checker and native consistency check. The historical-object workflow uses the same current checker before inspecting the two objects. The existing reader and scientific workflows are unchanged. The reader retains its tests, HTML build and actual loopback browser route; its test collection includes the new current-state checks. The scientific ready-PR and main gates run integrity, figure reproduction and `reproduce.py --full`, including all three complete certificate verifiers, the retained witness and analytical/physical controls. That existing command does not rebuild the rational partition from scratch; no rebuild is claimed.

## Verification record

Local checks used the recorded dependencies in the isolated Python 3.12.14 environment, assertions enabled, and OpenBLAS/OMP limited to one thread. Pandoc is 3.1.3 locally; CI uses the recorded 3.1.11.1. **175 tests passed**, including 24 mutation tests for the current preservation checker. The 378-file applied-candidate comparison preserves 364 files byte for byte and records 14 exact stage changes. All 27 approved graphics reproduce exactly.

| Command | Exit | Wall seconds |
|---|---:|---:|
| `python website/repository_preview.py --check` | 0 | 0.125 |
| `python verify.py` | 0 | 0.219 |
| `python -m pytest -q -p no:cacheprovider tests website/tests` | 0 | 12.005 |
| `python publication/integration/check_integrated.py --output build/merge-final-preservation.json` | 0 | 0.059 |
| `python reproduce.py --output build/merge-final-figures` | 0 | 6.249 |

The local HTML build and structural check also passed: 17 routes, 761 MathML expressions and all local links checked. Their exact commands, return codes and durations are in the committed merge-site-build and merge-site-check records. The final CI rebuild includes this report and checks the exact committed tree. No local browser success is claimed; actual loopback browser validation is required in the existing reader CI.

The first local test and figure runs failed because a prior untracked historical-review ZIP remained under build, and a proposed reader-workflow addition violated a retained workflow hash. The complete ZIP review directory was moved outside the checkout without changing its bytes, and reader-site.yml was restored exactly. Both original failed logs and their resolutions remain in the committed integration evidence. No assertion, expected result or tolerance changed. The dedicated integration workflow now runs the current and native checks.

Current-head remote outcomes belong to PR #5 and the linked actual Actions runs. Historical successful checks do not certify the new head, and pending, skipped or failed jobs are not passes.

The earlier candidate at `fa65dca` passed 151 tests and actual 17-route Chromium desktop/mobile/no-JavaScript checks in run 34686967212. The unpatched release reader passed 116 tests and its own browser run 34686967311. Those are inspected historical results, not substitutes for the current integration gates.

No new mathematical concern or scientific extension was introduced by this integration. No manuscript, public release, Pages deployment, visibility change or history rewrite is included. Historical disclosure and a later explicit visibility instruction remain separate decisions.
